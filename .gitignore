__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
examples/**/*_grid/
examples/**/processor*/
examples/**/postProcessing/
examples/**/*.foam
examples/**/log.*
# Commit the small (KB) fieldMinMax inputs for reproducibility.
# Full run outputs (postProcessing/, processor*/) stay ignored above;
# only the curated copies under each example's gci/data/ are tracked.
*.dat
!examples/**/gci/data/*.dat
